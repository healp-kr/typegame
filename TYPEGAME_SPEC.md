# 타자연습 프로그램 베이스 명세

## 1. 프로젝트 개요

HTML, CSS, JavaScript만 사용하여 브라우저에서 실행되는 타자연습 프로그램을 만든다.

- 별도의 서버와 데이터베이스는 사용하지 않는다.
- 파일 추가는 최소화한다.
- 기본 구성은 기존의 `exam4.html`, `exam4.css`, `exam4.js` 3개 파일을 유지한다.
- 별도 페이지를 여러 개 만들지 않고, 하나의 HTML 안에서 화면을 전환하는 SPA 형태로 구현한다.
- 기록은 브라우저의 `localStorage`에 저장한다.

## 2. 핵심 메뉴

첫 화면에는 다음 3개의 메뉴를 카드 또는 큰 버튼 형태로 표시한다.

1. 시간 측정
2. 타임 어택
3. 랭킹

각 화면에는 이전 화면으로 돌아갈 수 있는 `뒤로 가기` 버튼을 제공한다.

## 3. 전체 화면 흐름

```text
메인 화면
├─ 시간 측정
│  └─ 연습 문장 선택
│     └─ 타자 입력 화면
│        └─ 결과 화면 → 다시 하기 / 다른 문장 / 메인으로
├─ 타임 어택
│  └─ 연습 문장 선택
│     └─ 타자 입력 화면
│        └─ 결과 화면 → 다시 하기 / 다른 문장 / 메인으로
└─ 랭킹
   └─ 문장별 시간 측정 기록 확인 / 기록 초기화
```

## 4. 연습 문장

시간 측정과 타임 어택은 동일한 문장 선택 목록을 사용한다. 초기 버전은 3개 정도로 구성한다.

| ID | 화면 표시 이름 | 분류 | 내용 예시 |
|---|---|---|---|
| `anthem` | 애국가 | 한글 | 애국가 일부 또는 전체 |
| `korean-song` | 한국어 노래/창작문 | 한글 | 직접 작성하거나 사용 권한이 있는 한국어 문장 |
| `english-song` | 영어 노래/창작문 | 영어 | 직접 작성하거나 사용 권한이 있는 영어 문장 |

저작권이 있는 최신 노래 가사는 그대로 사용하지 않는다. 퍼블릭 도메인 문장, 직접 만든 문장, 또는 사용 허가를 받은 가사만 넣는다.

문장 데이터는 `exam4.js` 내부의 배열로 관리한다.

```js
const typingTexts = [
  {
    id: 'anthem',
    title: '애국가',
    language: 'ko',
    text: '연습에 사용할 문장'
  }
];
```

## 5. 시간 측정 모드

선택한 문장 전체를 정확하게 입력하는 데 걸린 시간을 측정한다.

### 시작 조건

- 문장 선택 후 연습 화면으로 이동한다.
- 입력 칸에 첫 글자가 입력되는 순간 타이머가 시작된다.
- 화면 진입만으로 타이머가 흐르지 않게 한다.

### 진행 규칙

- 원문과 입력문을 실시간으로 비교한다.
- 맞게 입력한 글자, 현재 입력 위치, 틀린 글자를 색상으로 구분한다.
- 붙여넣기는 차단하거나 무효 처리한다.
- 입력 중 경과 시간을 초 단위로 표시하되 내부 계산은 밀리초 단위를 사용한다.
- 모든 글자를 정확히 입력했을 때 자동으로 종료한다.

### 결과

- 총 소요 시간
- 분당 타수(CPM)
- 정확도
- 최고 기록 갱신 여부

완료 기록은 선택한 문장별로 `localStorage`에 저장한다. 기본 랭킹 기준은 소요 시간이 짧은 순서이며, 문장별 상위 10개 기록만 보관한다.

## 6. 타임 어택 모드

정해진 시간 동안 선택한 문장을 앞에서부터 최대한 많이 입력한다.

### 기본 설정

- 제한 시간: 60초
- 첫 글자 입력 시 카운트다운 시작
- 남은 시간을 화면에 크게 표시

### 진행 규칙

- 선택한 원문을 순서대로 입력한다.
- 원문 끝까지 입력하면 처음부터 이어서 반복할 수 있다.
- 맞게 입력한 글자 수만 최종 타수에 포함한다.
- 제한 시간이 0초가 되면 입력을 비활성화하고 자동 종료한다.
- 붙여넣기는 차단하거나 무효 처리한다.

### 결과

- 정확하게 입력한 글자 수
- 분당 타수(CPM)
- 정확도
- 오타 수

초기 버전의 랭킹은 시간 측정 기록만 다루며, 타임 어택 결과는 결과 화면에만 보여준다.

## 7. 랭킹 화면

시간 측정 모드에서 완료한 기록을 문장별로 보여준다.

### 표시 항목

- 순위
- 문장 이름
- 소요 시간
- 분당 타수
- 정확도
- 플레이 날짜

### 동작

- 문장별 필터 또는 탭을 제공한다.
- 기록이 없으면 `아직 기록이 없습니다.`를 표시한다.
- `전체 기록 초기화` 버튼을 제공한다.
- 초기화 전 `confirm`으로 한 번 확인한다.

## 8. 데이터 저장 구조

`localStorage` 키는 버전 관리를 고려하여 다음과 같이 사용한다.

```js
const STORAGE_KEY = 'typingGame.rankings.v1';
```

저장 데이터 예시:

```json
{
  "anthem": [
    {
      "timeMs": 82450,
      "cpm": 312,
      "accuracy": 98.7,
      "playedAt": "2026-09-04T12:00:00.000Z"
    }
  ]
}
```

- 기록 저장 후 `timeMs` 오름차순으로 정렬한다.
- 문장마다 상위 10개만 남긴다.
- 데이터가 없거나 손상된 경우 빈 객체로 안전하게 초기화한다.

## 9. 타자 판정 및 통계 기준

한글 조합 입력을 지원하기 위해 `keydown`으로 글자를 하나씩 판정하지 않는다. `input` 또는 `compositionend` 이벤트에서 `textarea.value`와 원문을 비교한다.

### 문자 비교

- 공백, 줄바꿈, 문장부호도 하나의 문자로 취급한다.
- 입력값의 각 위치를 원문의 같은 위치와 비교한다.
- 원문보다 긴 입력은 허용하지 않거나 초과 부분을 잘라낸다.

### 정확도

```text
정확도(%) = 올바르게 입력한 횟수 / 전체 입력 횟수 × 100
```

오타를 지웠다가 고쳐도 이미 발생한 오타는 전체 입력 횟수와 오타 수에 반영한다. 첫 버전에서 구현이 복잡하면 완료 시점의 원문 대비 일치율을 임시 기준으로 사용할 수 있으나, 화면과 코드에서 기준을 일관되게 유지한다.

### 분당 타수

```text
CPM = 정확하게 입력한 글자 수 / 경과 시간(초) × 60
```

## 10. 화면 구성

### 공통

- 프로그램 제목
- 현재 모드와 선택 문장 표시
- 키보드만으로도 주요 기능을 사용할 수 있는 포커스 처리
- 모바일과 데스크톱에서 모두 사용할 수 있는 반응형 레이아웃

### 메인 화면

- `시간 측정`, `타임 어택`, `랭킹` 카드 3개
- 각 모드에 대한 한 줄 설명

### 문장 선택 화면

- 문장 제목
- 언어 또는 분류
- 문장 길이
- 시작 버튼

### 연습 화면

- 원문 표시 영역
- 입력 위치가 잘 보이는 문자 강조
- 입력용 `textarea`
- 시간, 타수, 정확도 등의 실시간 정보
- 다시 시작 및 나가기 버튼

### 결과 화면

- 핵심 결과를 큰 숫자로 표시
- 최고 기록이면 별도 강조
- 다시 하기 / 다른 문장 선택 / 메인으로 버튼

## 11. 디자인 방향

- 집중을 방해하지 않는 단순하고 깔끔한 디자인
- 상태 색상 예시:
  - 입력 완료: 초록색 또는 흐린 회색
  - 현재 위치: 강조색과 밑줄
  - 오타: 빨간색
- 카드와 버튼의 hover, focus 상태를 제공한다.
- 색상만으로 상태를 전달하지 않도록 밑줄, 굵기 등의 표현을 함께 사용한다.
- 작은 화면에서는 메뉴 카드와 결과 항목을 한 열로 배치한다.

## 12. JavaScript 상태 예시

전역 변수의 수를 줄이고 하나의 상태 객체로 게임 상태를 관리한다.

```js
const state = {
  screen: 'home',
  mode: null,
  selectedTextId: null,
  startedAt: null,
  timerId: null,
  typedValue: '',
  correctCount: 0,
  totalInputCount: 0,
  errorCount: 0,
  isRunning: false
};
```

화면이 바뀌거나 게임을 다시 시작할 때 기존 `setInterval`을 반드시 해제하여 타이머가 중복 실행되지 않게 한다.

## 13. 권장 함수 구성

```text
showScreen(screenName)
renderTextChoices(mode)
startPractice(textId)
handleTypingInput(event)
renderTypingProgress()
startTimer()
finishGame()
calculateResult()
loadRankings()
saveRanking(result)
renderRankings(filterId)
resetRankings()
resetGameState()
```

## 14. 접근성 및 사용성

- 모든 버튼에 이해하기 쉬운 텍스트를 사용한다.
- 입력 칸에는 연결된 `label` 또는 `aria-label`을 제공한다.
- 키보드 포커스 표시를 제거하지 않는다.
- 문장 선택 후 입력 화면이 열리면 `textarea`에 자동 포커스한다.
- 게임 종료 시 결과 영역으로 포커스를 이동하거나 스크린 리더가 결과를 알 수 있게 한다.
- 사용자가 게임 도중 나가려 할 때 진행 기록이 사라짐을 안내한다.

## 15. 예외 처리

- 새로고침하면 진행 중인 게임은 종료하고 메인 화면부터 시작한다.
- 탭이 백그라운드로 이동해도 시간은 `setInterval` 횟수가 아니라 실제 시각 차이로 계산한다.
- 제한 시간이 끝난 뒤 추가 입력이 결과에 반영되지 않게 한다.
- 매우 빠른 연속 클릭으로 타이머가 여러 개 생성되지 않게 한다.
- `localStorage` 사용이 불가능한 환경에서도 게임 자체는 실행되게 한다.

## 16. MVP 완료 기준

- [ ] 첫 화면에 3개의 메뉴가 표시된다.
- [ ] 시간 측정과 타임 어택에서 같은 문장 목록을 선택할 수 있다.
- [ ] 한글과 영어를 정상적으로 입력하고 실시간 비교할 수 있다.
- [ ] 시간 측정은 첫 입력부터 완성까지 걸린 시간을 계산한다.
- [ ] 타임 어택은 60초 후 정확히 종료된다.
- [ ] 두 모드 모두 결과 화면에서 타수와 정확도를 보여준다.
- [ ] 시간 측정 기록이 문장별로 저장되고 랭킹에 표시된다.
- [ ] 기록 전체 초기화가 동작한다.
- [ ] 새로고침 후에도 랭킹 기록이 유지된다.
- [ ] PC와 모바일 화면에서 레이아웃이 깨지지 않는다.
- [ ] 개발자 도구 콘솔에 오류가 없다.

## 17. 구현 우선순위

1. 기존 HTML의 깨진 한글 인코딩을 UTF-8로 정리한다.
2. 메인 화면과 화면 전환 구조를 만든다.
3. 공통 문장 데이터와 문장 선택 화면을 만든다.
4. 시간 측정 모드와 결과 계산을 구현한다.
5. 타임 어택 모드를 구현한다.
6. `localStorage` 랭킹을 구현한다.
7. 반응형 디자인과 접근성을 다듬는다.
8. 한글 조합 입력, 타이머 중복, 새로고침 등의 예외 상황을 테스트한다.

## 18. 초기 버전에서 제외할 기능

다음 기능은 파일과 코드 규모를 불필요하게 키울 수 있으므로 MVP 이후로 미룬다.

- 회원가입과 로그인
- 서버 기반 전체 사용자 랭킹
- 사용자 문장 업로드
- 온라인 멀티플레이
- 효과음 및 배경 음악 파일
- 프레임워크와 외부 라이브러리
- 난이도 자동 추천

